#!/bin/sh
# Auto-update the docs version marker (date + content fingerprint) on every
# commit, so docs.apiany.ai always shows a marker matching the committed content.
# Enable once with: git config core.hooksPath .githooks  (pnpm install does this).

node scripts/update-docs-version.mjs || exit 1
git add snippets/docs-version.mdx
